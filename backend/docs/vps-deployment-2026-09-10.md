# Scorebook VPS 部署与验收

2026-09-10 23:04（北京时间）完成部署与隔离验收。

## 直接打开

双击桌面的 [打开Scorebook_VPS.command](/Users/mdd/Desktop/打开Scorebook_VPS.command)，脚本会建立 SSH 隧道并打开浏览器。

访问地址：[VPS 版 Scorebook](http://127.0.0.1:5179/#/home)。5179 是 VPS 版，5178 仍是 Mac 本地版。后端凭证不进入浏览器；当前使用 SSH 身份验证，没有公开到互联网的免登录入口。

## 已修复

- 复盘深链接准确打开指定记录，不再选到队列第一页的其他记录；队列读取失败也不阻断指定记录。
- 已发布过复盘后又写新草稿，下一步会正确提示继续；结果变更仍优先复核。
- “查看结果 / 沉淀做法”会定位到对应部分，已作废记录不显示不适用的下一步。
- 从找相似进入历史准备时，保留周期、品种与市场，覆盖范围也按当前条件查询。
- 截图、框选、周期、范围和同一次搜索任务引用可以在刷新后恢复；不会保存原始行情或结果图到浏览器存储。
- 防止重复提交、旧轮询覆盖新查询；提交后离开再返回，由当前页面接手迟到的响应。轮询结束释放等待清理项。
- Linux OCR 使用独立标题区域识别，避免深色 K 线干扰标题分区；坐标与现有前端协议统一。未降低识别置信度，未知周期仍必须用户选择。

## VPS 部署

107.174.172.10，目录 `/opt/scorebook`，五个 systemd 服务全部 active：API、worker、前端、DINOv2、BGE-M3。独立 PostgreSQL 17 + pgvector 0.8.2，迁移版本 41。所有 Scorebook 端口仅监听回环地址。

正式库最终核对：记录 0、复盘 0、附件 0、公共历史特征 0、历史订阅 0、临时验收数据库 0。没有导入 Mac 的个人资料，没有启动历史自动同步。

## 订单流清理

已删除 70 张明确属于订单流的表、1,032 条订单流 outbox 记录及对应 audit 行、9 个停用订单流容器、NATS 专属数据卷、旧订单流镜像和相关停用单元、`/opt/bit-orderflow` 与 `/opt/bit-orderflow-database-phase0a`。没有制作备份。

保留实盘监控容器、共享 PostgreSQL 卷及监控数据；清理事务验证保留数据指纹一致。实盘监控与数据库没有重启，原先停用的实盘通知 outbox 继续停用。无法明确划归订单流的共享收据保留。FOMO、Telegram、炸金花和其他项目未改动。两台 VPS 的 cron、systemd 自动启动配置复核未发现残留订单流启动项，订单流不再运行或自动恢复。

最终复查：实盘监控最近 30 分钟 60 行日志没有 error/fatal/panic/traceback；FOMO active，Telegram healthy。107 磁盘占用从约 94% 降至约 11%，可用约 123 GiB。

## 验证结果

- 后端工作区：96 项通过、0 失败；8 项外部环境用例未在此命令中运行。Clippy（warnings 作为错误）、格式与边界检查通过。
- 前端：7 项针对本轮修复的回归测试通过；TypeScript 与 Vite 构建通过。
- BFF：同源代理、资产读取、凭证不暴露等测试通过。
- VPS 真数据验收：明确使用币安官方月档，BTC / ETH、2026-08、4h。完成校验和与完整覆盖检查、Linux OCR、DINOv2 384 维与 BGE-M3 1024 维实际推理、周期必选、16 条几何特征建立、同周期匹配和两个不同品种结果的 SVG 重绘。
- 验收结束立即删除临时库、截图和 16 条特征。回执只保留元数据：[VPS 验收回执](/Users/mdd/zhk/scorebook-backend/docs/vps-acceptance-2026-09-10.json)。这项测试不等同于真实用户截图盲测质量验收。

## 仍然受限的能力

**107 VPS 直连 Binance Futures REST 返回 HTTP 451。** 因此实时行情、24h 成交量排行、合约目录刷新以及依赖 REST 的历史/结果核验暂时不能正常使用。当前空库也没有本地合约目录可供替代。没有静默换交易所或恢复旧代理；明确选择“官方月度归档”的取数、匹配与重绘路径已验证可用。

Chat 生成模型、交易所账户密钥和加密备份尚未配置。Linux OCR 当前为英文识别；不可靠或未知的品种/周期不自动填写。

## 发行物

- 后端 Linux 二进制 SHA-256：`c70ef4e8953f66cfd3b847156b61851d02d8c04f2f6b24f26e0339060bacf51f`
- 前端 `index-DK4NJd43.js` SHA-256：`8bf92c2bbd248f580783eac7c95e3e7b4f6ca607ed0a9b2523a4892ea8f7af92`
- 前端最终部署包 SHA-256：`439dff34c9f186b3812eb928366c1689d973894690bae34e969e5f02dec7c6fe`
- 运维与重装说明：[deployment-vps.md](/Users/mdd/zhk/scorebook-backend/docs/deployment-vps.md)。部署来自现有工作树，保留 Claude 与此前后端改动，未清理或重置本地 Git 工作树。
