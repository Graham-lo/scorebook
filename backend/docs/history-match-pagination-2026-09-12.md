# 历史匹配分页与报价币去重（2026-09-12）

用户确认的行为：历史相似结果统一称为「历史匹配」，上一批/下一批分页；已看过的页面可返回；按匹配度排序；匹配图不再叠加查询截图虚线。

实现：一次搜索完成有界候选池的精排后，保存去重后的 ranked_items，items 保持兼容的首页限制。前端按每页 3/5/10 条切片，同一轮翻页不创建搜索、不追加否定条件；页面状态在本次应用会话内保留。结果仅涵盖本次已核验候选池，并非无限分页遍历币安全量历史。私库融合检索继续保留已有文字/图像融合排序。

品种（用户最新要求优先）：历史匹配 USDⓈ-M 只采用 USDT 计价合约，在精排取 OHLC 前筛掉其它报价币，旧结果的展示也遵守此规则。仅接受目录确认，不靠后缀猜测；不重写已有原始定位。校准参考池保留原有口径，不触发额外全量重算。

去重实现：使用本地币安 instrument_catalog 的 baseAsset / quoteAsset / contractType；仅 USDⓈ-M 下同标的 USDT/USDC 永续、同周期、窗口重叠超过较短窗口一半时去重，保留高匹配分的真实合约。不按字符串后缀猜基础资产，不改变真实 OHLC、交易对、截图定位。旧搜索读取时也对展示结果去重，不改写存档证据。

存储约定保持：长期保存原截图/用户记录/定位标记/索引特征/搜索坐标与匹配评分，不长期保存完整原始行情。历史匹配图由后端按坐标拉取，OHLC 和生成图只在内存中使用。重温优先前端直连，后端回退使用 replay_bars 临时表，每次使用续期24小时，退出时清理，定时清理过期项。

验证：前端83单测与生产构建通过；新增后端测试覆盖报价币重复、保留高分合约、非重叠/不同市场周期/目录缺失不误合并、旧结果只读投影与账户隔离；前端测试覆盖前后翻页顺序、末页、每页条数变化与空结果。后端全量测试通过（10项环境依赖测试按原约定忽略），clippy零警告；后续USDT规则的定向测试通过。浏览器验证见下。

## 最终部署与浏览器验收

- 本机 API/worker 已 release 构建并重启；前端生产构建并重启。入口 http://127.0.0.1:5178/。
- 新真实搜索 `09c3f47c-6f57-4518-b77d-1acb62c99ae5`：35条，全部目录确认USDT，合约不重复，匹配分降序。`quote_policy=usd_m_usdt_only_v1`，`pagination=frozen_verified_ranking_v1`。
- CUA 浏览器真实桌面/手机验收：七页从1连续翻到7再返回1，每页5条，末页下一批禁用；返回第一页恢复原五条。没有新增检索来翻页。
- BTCUSDT 放大图显示113根真实K线及成交量，没有查询截图轮廓虚线；保留十字线、价格线与匹配截止标记。
- 手机390px搜索栏原有横向溢出已用局部discover.css规则修复，scrollWidth=390。分页移至结果顶部并设置滚动顶部间距，避免底栏遮住按钮；已复验完整七页来回。
- 前端最新资产 index-CCtqA2OQ.js / index-CPyDoFNV.css。保留工作树原有其它样式修改，未提交或推送。
