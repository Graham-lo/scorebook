# Scorebook 本轮交付与验收 · 2026-09-12

> 后续截图复用、全屏图表与新版视觉已完成，最新行为、测试数量和产物见 [后续验收](screenshot-charts-followup-2026-09-12.md)。下文保留早期交付时点。

更新时间：2026-09-12T18:59:36+08:00。代码、独立审查发现的修复及本机部署已完成；行情索引与正式分数校准仍在后台续跑，不把这两项标为完成。

## 运行位置

- 后端 `scorebook-backend`，分支 `codex/deploy-scorebook-local`。
- 前端 `scorebook-frontend/app`，仓库分支 `main`。
- 本机前端 http://127.0.0.1:5178 ，API 8787。`scorebook/main` 是较旧的合并仓库，本轮未修改；`zjh-codex-p3` 属于别的项目。
- 前端生产产物 `index-Cv0HT58F.js` / `index-DgT_6kyw.css`，构建后重启服务并核对实际 HTTP 返回的哈希。后端 release 构建、迁移 51–53、API/worker 重启完成，健康端点 200 / ok。

## 交付内容

保留并完成 Claude 已有的信息架构和页面重构。五个主入口为今天、记录、复盘、经验、找；新建三步、重温五步。独立审查先检查现存源码，之后才与 Claude redesign §8 / screen-map 交叉认证，详见同目录 `independent-product-audit-2026-09-12.md`。

- 修复审查报告中的 20 类问题：搜索上下文与失败反馈、上传竞态、录入有效性与草稿恢复、首次账户入口、筛选分页和缓存、复盘保存冲突、记录行键盘行为、附件身份、历史图时间、分类版本与真实关联、队列统计口径、账单竞态、设置能力与任务管理、深链接、模态键盘与手机溢出。
- 截图定位支持确认前只读预览。多张图各自保留品种、周期、定位窗口和原图。真实 OHLC 图支持缩放、拖动、触摸缩放、复位、指标、逐根/键盘回放，搜索结果支持后续 32/64/128 根。后续走势仅用于展示，不进入匹配评分。
- 重温默认从图中窗口开始，另可选择判断时刻；判断当时未收盘根的最终 OHLC 不会提前泄露。换轨按时间保持进度；隐藏页面暂停播放。
- 统一截图和文字请求：私有记录使用 BGE-M3 语义/文字检索与图形候选融合，保留来源、命中理由和引用。公开行情仍按图形检索，界面分组说明来源。识别出的周期也可修改，支持输入法和失败重试。
- 公共候选缺少相邻月归档时先核验原窗口，确实缺失则排除该组，不再导致整轮结果失败。
- 私有 DINO 查询与存量特征统一主图裁剪 profile，已重建 3 张现有 scene。
- 校准改为正式候选与精排 300 流水线，独立版本、逐样本持久化、可重启续跑；旧 top3 样本不混入。

## 验证证据

| 检查 | 结果与边界 |
|---|---|
| 后端隔离数据库全套 | 211 通过，0 失败，10 忽略；测试库已删除 |
| 新增噪声图片验收 | 另 1 项通过；unreadable、零候选、不写定位 |
| 真实截图冷启动 | 新附件和隔离库跑 MU / SKHY；不是复用 already_located。MU extremes 自动定位，end 09-08 23:00Z；SKHY time_axis 候选，end 09-09 13:00Z，不声称它自动接受成功 |
| 后端静态检查 | clippy workspace/all-targets 零警告，release 构建通过 |
| 前端 | 76 单测通过、类型检查与生产构建通过 |
| 页面扫描 | 1280 / 375 / 320 三宽度共 57 视图；无脚本异常或文档横向溢出 |
| 定向交互 | 14 组录入/账单竞态/失败/分页/分类/设置等检查通过；其中模拟响应的项目在证据中明确 marked mocked，不计为真实后台写入验收 |
| 最终图表与搜索交互 | 7 组检查通过：截图截止、判断截止、缩放拖动、换轨、逐根键盘、文字图片上下文/周期修改、后续32根及Esc退出。搜索 UI 使用此前真实运行结果作固定响应；图表取真实 OHLC |
| 真实公开搜索 | 新 run `4feeca5e-cf11-44f4-ad04-506994fb151b` succeeded；top MUUSDT / sure / 0.8832120886；rarity=null，符合未完成校准的降级行为 |

持久化本机证据：项目根 `.project-memory/evidence-2026-09-12/`，保留测试输出、扫描/交互 JSON、真实搜索与冷启动结果、最终手机截图。真实私有记录和截图证据不放入 Git 仓库。

## 仍在运行和能力边界

- 正式校准需要 1h / 4h / 1d × 64 / 96 / 128 / 192 / 256 共 15 档，每档 500 个有效样本。当前快照 313 / 7500，没有任何一档达到 500。后台 worker 会继续执行，满额的档位才启用稀有度；其余使用绝对分数档。样本数是这次读库的快照，不是固定进度或剩余时间预测。
- 行情 96/192 新档位的历史索引仍有任务续跑；前端展示实际覆盖，不承诺尚未准备的范围。
- 本机 chat_generation 未配置；当前不展示不可用的生成式回答入口。BGE-M3 文本语义检索已配置。联合搜索已具备，不能把它描述成所有生成式能力都已开启。
- 用户确认 Mac 已恢复登录。已停止额外审查代理；只保留原生「Scorebook 独立产品与交互深度审查」线程，明确禁止它再派生线程。它曾停在浏览器工具等待状态，不能用该线程的状态冒充本报告的独立源码审查证据。
- Claude 原会话已通知交接，自动恢复关闭；本轮不会在 18:10 之后恢复并发改动。是否阅读通知不作无证据推断。
