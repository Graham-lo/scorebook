# v4 实施状态（持续施工，尚未部署）

2026-09-10。分支 codex/backend-personal-v4，基线 eccd1f9。只做 P1/P2/P3/P4/P5/P7；P6、前端、多用户产品功能排除。现有主服务和主数据库保持 v3。

## 已落代码并进入验证

- P1：截图 ROI、Apple Vision OCR 适配器、candle-geometry-v2、局部/历史候选搜索、DTW 排序、持久任务与取消；原始公共行情只在内存使用。
- P2：USD-M/COIN-M 现行/归档合约目录、月 ZIP 校验读取、分市场/周期分区向量、三尺度订阅、容量预算、断点任务。真实公开 BTCUSDT 2024-01 1h 归档 744 根校验通过。
- P3：只读账户 API、CSV/规范化批量导入、幂等不可变流水、Decimal 持仓轮次/反手/双向/币本位、未知期初标记、分币种对账、显式执行关联。1000 条同毫秒翻页与三次重复导入测试通过。增量测试：新增 1 笔只处理 1 笔，冻结闭合轮次复用；补入更早成交只重建受影响账本。导出未知提交重复重试不再申请，1001 行分批只排一次投影测试通过。
- P4：共享规则聚合器、T3 持续游标与精确触发、固定统计成员/代表与来源版本、T1 B1 历史背景参照、人工裁决请求、打法/标签/episode 复盘语义。触发边界和固定成员测试通过。
- P5：固定版本本地 BGE-M3 中文语义索引、批量 outbox、混合召回、源版本引用；供应商中立 Chat 工具编排、持久轮次、权限/写入确认、SSE 和同步删除失效。真实本地编码器 + 数据库语义检索测试通过；脚本模型验证 Chat 重试/删除流程。
- P7：固定版本 Restic、Keychain 端口、30 分钟调度、导出冻结/复制保护、上传后仓库读回校验、保留策略、恢复命令；旧 v19 归档显式离线升级，严格拒绝未经升级的旧归档。真实 Restic 临时加密仓库 → 读回 → 独立空数据库恢复及错误密码拒绝测试通过（11.75 秒，少量合成资料）。

## 尚需完成的收尾与验收

- P1/P2：旧模型显式切换、目录边界/修订窗口、搜索复用与大库性能、真实截图质量集。
- P3：已补历史导出额度/未知提交处理、连接断开/换钥、按持仓账本增量投影；仍需真实账户/CSV格式与大账本验收。
- P4：共享行情监控与显式归档补齐、统计边界与真实 B1 验收。
- P5：完整来源/长文分页、实际 Chat 供应商与图片输入、真实问题验收集。
- P7：独立备份位置及真实资料恢复演练；少量本机合成测试不能代替故障恢复能力验收。
- 统一契约、能力状态、数据清理、混合故障与全量回归、最终部署手册。

## 外部条件（不能伪称已验收）

尚未选择/配置真实 Chat 供应商，没有真实交易所只读密钥，没有独立备份目的地。未创建或复用外部 API 密钥。后台已有流程不等于这些外部接入已完成。

## 数据边界

用户原图、原话、复盘、真实交易流水是资产。公共 OHLC/aggTrades/系统绘图不进数据库、任务 payload、日志、聊天或备份；仅存派生特征、摘要、哈希、坐标。无静默 provider/model/algorithm fallback。每项未具备条件返回明确状态。
